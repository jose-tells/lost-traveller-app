import React from 'react';

const UserProfileSections = ({ children }) => {

  return (
    <div className='UserProfileSections__container'>
      {children}
    </div>
  );
};

export default UserProfileSections;
